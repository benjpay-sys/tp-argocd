# Surcharges pour l'environnement de développement (namespace devhub-dev).
# Ne mettez ICI que ce qui diffère de values.yaml.

image:
  repository: host.docker.internal:5001/planning
  tag: tp3-202606170940
  pullPolicy: Always

ingress:
  enabled: true
  host: planning.devhub.local

env:
  LOG_LEVEL: debug

monitoring:
  enabled: true
